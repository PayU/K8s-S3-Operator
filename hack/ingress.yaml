apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: localstack-ingress
spec:
  ingressClassName: kong
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: localstack
            port:
              number: 4566